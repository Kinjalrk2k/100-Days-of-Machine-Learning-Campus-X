# DAY-29

Pipeline in scikit-learn chains together multiple steps so that the output of each step is used as an input in the next step.

Pipelines makes it easy to apply the same pre-processing to train and test!
